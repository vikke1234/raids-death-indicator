# Reload changed .class files automatically without an IDE/debugger trigger.
# This is what makes `./gradlew compileJava` live-reload into a running runClient.
autoHotswap=true

# Uncomment to see every file-watch / reload decision in the runClient console:
#LOGGER=debug
